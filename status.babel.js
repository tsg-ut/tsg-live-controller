const React = require('react');
const ReactDOM = require('react-dom');

const Status = require('./Status.jsx');

const app = document.getElementById('app');
ReactDOM.render(React.createElement(Status), app);
